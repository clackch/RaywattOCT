<Page x:Class="RaywattApp.Views.ReviewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:local="clr-namespace:RaywattApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ReviewPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Background="Gray">
            <Label Content="{Binding [Review], Source={StaticResource L10N}}"></Label>
        </StackPanel>

        <StackPanel Grid.Row="1">
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" >
                <StackPanel Orientation="Horizontal">
                    <Button Width="80" Command="{Binding EditCaseCommand}" Content="{Binding [Edit Case], Source={StaticResource L10N}}" Margin="5,5,5,5"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="{Binding [Vessel], Source={StaticResource L10N}}"></Label>
                    <ComboBox Width="100" Name="vesselComboBox" ItemsSource="{Binding VesselComboBox}" SelectedValue="{Binding CurrentVessel, Mode=TwoWay}" SelectedValuePath="Key" DisplayMemberPath="Value" Margin="5,5,5,5">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding VesselChangedCommand}" CommandParameter="{Binding ElementName=vesselComboBox, Path=SelectedItem}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="DropDownOpened">
                                <i:InvokeCommandAction Command="{Binding VesselOpenedCommand}" CommandParameter="{Binding ElementName=vesselComboBox, Path=SelectedItem}"/>
                            </i:EventTrigger>
                            <i:EventTrigger EventName="DropDownClosed">
                                <i:InvokeCommandAction Command="{Binding VesselClosedCommand}" CommandParameter="{Binding ElementName=vesselComboBox, Path=SelectedItem}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </ComboBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="{Binding [Procedure], Source={StaticResource L10N}}"></Label>
                    <ComboBox Width="100" ItemsSource="{Binding ProcedureComboBox}" SelectedValue="{Binding CurrentProcedure, Mode=TwoWay}" SelectedValuePath="Key" DisplayMemberPath="Value" Margin="5,5,5,5"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" >
                <Button Width="80" Command="{Binding EndReviewCommand}" Content="{Binding [End Review], Source={StaticResource L10N}}" Margin="5,5,5,5"/>
            </StackPanel>

        </Grid>
    </Grid>
</Page>
