The folder w32 contains the same files as this (.\lib) folder to allow existing applications
to build without changes.

It is recommended to change your build configurations to use the lib folder as this legacy w32
folder will be removed in a future release.  