<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
                    xmlns:component="clr-namespace:RaywattOCTFFR.Views.Component">

    <!-- Icon -->
    <Style TargetType="Image" x:Key="RaywattOutset">
        <Setter Property="Width" Value="383.1"/>
        <Setter Property="Height" Value="220"/>
        <Setter Property="Source" Value="{svgc:SvgImage Source=/res/icon/raywattOutset.svg}"/>
    </Style>

    <Style TargetType="Image" x:Key="Raywatt">
        <Setter Property="Width" Value="437.3"/>
        <Setter Property="Height" Value="95"/>
        <Setter Property="Source" Value="{svgc:SvgImage Source=/res/icon/raywatt.svg}"/>
    </Style>

    <Style TargetType="Image" x:Key="RaywattSmall_Icon">
        <Setter Property="Width" Value="91.1"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Margin" Value="16, 10, 0, 10"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Source" Value="{svgc:SvgImage Source=/res/icon/raywattSmall.svg}"/>
    </Style>

    <Style TargetType="Image" x:Key="Faster">
        <Setter Property="Width" Value="450"/>
        <Setter Property="Height" Value="125.8"/>
        <Setter Property="Source" Value="{svgc:SvgImage Source=/res/icon/faster.svg}"/>
    </Style>

    <Style TargetType="{x:Type component:IconButton}" x:Key="IconButton">
        <Setter Property="Width" Value="26"/>
        <Setter Property="Height" Value="26"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type component:IconButton}">
                    <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Background="Transparent">
                        <Image x:Name="icon" Source="{Binding IconDefault, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="True">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconDefault, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconOver, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconPressed, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconDisabled, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type component:IconButton}" x:Key="IconButtonLarge" BasedOn="{StaticResource IconButton}">
        <Setter Property="Width" Value="42"/>
        <Setter Property="Height" Value="42"/>
    </Style>
    
    <Style TargetType="{x:Type component:IconRepeatButton}" x:Key="IconRepeatButton">
        <Setter Property="Width" Value="26"/>
        <Setter Property="Height" Value="26"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type component:IconRepeatButton}">
                    <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Background="Transparent">
                        <Image x:Name="icon" Source="{Binding IconDefault, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="True">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconDefault, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconOver, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconPressed, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconDisabled, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type component:IconToggleButton}" x:Key="IconToggleButton">
        <Setter Property="Width" Value="26"/>
        <Setter Property="Height" Value="26"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type component:IconToggleButton}">
                    <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Background="Transparent">
                        <Image x:Name="icon" Source="{Binding IconDefault, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="True">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconDefault, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconOver, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconPressed, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconDisabled, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type component:IconMultiToggleButton}" x:Key="IconMultiToggleButton">
        <Setter Property="Width" Value="26"/>
        <Setter Property="Height" Value="26"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type component:IconMultiToggleButton}">
                    <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Background="Transparent">
                        <Image x:Name="icon" Source="{Binding IconDefault, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="False"/>
                                <Condition Property="IsMouseOver" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconDefault, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="False"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="icon" Property="Source" Value="{Binding IconOver, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="IsMouseOver" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="icon" Property="Source" Value="{Binding CheckedIconDefault, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="icon" Property="Source" Value="{Binding CheckedIconOver, Converter={svgc:SvgImageConverter}, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Indicator -->
    <Style TargetType="{x:Type component:IndicatorImage}" x:Key="IndicatorImage">
        <Setter Property="Source" Value="{Binding IconDefault, Converter={svgc:SvgImageConverter}, RelativeSource={x:Static RelativeSource.Self}}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Source" Value="{Binding IconOver, Converter={svgc:SvgImageConverter}, RelativeSource={x:Static RelativeSource.Self}}"/>
                <Setter Property="Cursor" Value="{StaticResource hand}"/>
            </Trigger>
            <Trigger Property="IsCaptured" Value="True">
                <Setter Property="Source" Value="{Binding IconOver, Converter={svgc:SvgImageConverter}, RelativeSource={x:Static RelativeSource.Self}}"/>
                <Setter Property="Cursor" Value="{StaticResource grab}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Source" Value="{Binding IconDisabled, Converter={svgc:SvgImageConverter}, RelativeSource={x:Static RelativeSource.Self}}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Image" x:Key="SectionLeftRight">
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="123"/>
        <Setter Property="Margin" Value="0, -18, 0, 0"/>
        <Setter Property="Source" Value="{svgc:SvgImage Source=/res/indicator/sectionLeft.svg}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="Right">
                <Setter Property="Source" Value="{svgc:SvgImage Source=/res/indicator/sectionRight.svg}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Image" x:Key="SectionCompareLeftRight">
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="188"/>
        <Setter Property="Margin" Value="0, -18, 0, 0"/>
        <Setter Property="Source" Value="{svgc:SvgImage Source=/res/indicator/sectionCompareLeft.svg}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="Right">
                <Setter Property="Source" Value="{svgc:SvgImage Source=/res/indicator/sectionCompareRight.svg}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Image" x:Key="SectionExportLeftRight">
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="298"/>
        <Setter Property="Margin" Value="0, -18, 0, 0"/>
        <Setter Property="Source" Value="{svgc:SvgImage Source=/res/indicator/sectionExportLeft.svg}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="Right">
                <Setter Property="Source" Value="{svgc:SvgImage Source=/res/indicator/sectionExportRight.svg}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Progress Bar -->
    <Style TargetType="{x:Type ProgressBar}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border BorderThickness="0" CornerRadius="4" Background="{DynamicResource GrayscaleGray7Brush}"/>
                        <Rectangle Name="PART_Track"/>
                        <Border Name="PART_Indicator" CornerRadius="4" Background="{DynamicResource MainPrimaryColorBrush}" HorizontalAlignment="Left"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ProgressBar}" x:Key="LoadingProgressBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border BorderThickness="0" CornerRadius="6" Background="{DynamicResource GrayscaleGray8Brush}"/>
                        <Rectangle Name="PART_Track"/>
                        <Border Name="PART_Indicator" CornerRadius="6" Background="{DynamicResource MainPrimaryColorBrush}" HorizontalAlignment="Left"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ProgressBar}" x:Key="CatheterProgressBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border Background="{DynamicResource GrayscaleGray3Brush}" Margin="1,0,1,0"/>
                        <Rectangle Name="PART_Track"/>
                        <Border Name="PART_Indicator" Background="{DynamicResource MainPrimaryColorBrush}" HorizontalAlignment="Left" Margin="1,0,1,0"/>
                        <Image x:Name="img" Source="{svgc:SvgImage Source=/res/icon/catheterConnect.svg}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ProgressBar}" x:Key="HomeCatheterProgressBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border Background="{DynamicResource GrayscaleGray3Brush}" Margin="1,0,1,0"/>
                        <Rectangle Name="PART_Track"/>
                        <Border Name="PART_Indicator" Background="{DynamicResource MainPrimaryColorBrush}" HorizontalAlignment="Left" Margin="1,0,1,0"/>
                        <Image x:Name="img" Source="{svgc:SvgImage Source=/res/icon/catheterConnect_home.svg}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ProgressBar}" x:Key="FfrProgressBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border Background="{DynamicResource GrayscaleGray8Brush}" Margin="1,0,1,0"/>
                        <Rectangle Name="PART_Track"/>
                        <Border Name="PART_Indicator" Background="{DynamicResource MainPrimaryColorBrush}" Opacity="0.25" HorizontalAlignment="Left" Margin="1,0,1,0"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Loading -->
    <ControlTemplate x:Key="Loading">
        <Ellipse Width="40" Height="40" StrokeThickness="4">
            <Ellipse.Stroke>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#1ed760" Offset="0.25"/>
                    <GradientStop Color="#161616" Offset="0.75"/>
                </LinearGradientBrush>
            </Ellipse.Stroke>
            <Ellipse.Triggers>
                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetName="rotateTransform"
                                Storyboard.TargetProperty="Angle"
                                From="0" To="360"
                                Duration="0:0:1"
                                RepeatBehavior="Forever"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Ellipse.Triggers>
            <Ellipse.RenderTransform>
                <RotateTransform x:Name="rotateTransform" CenterX="20" CenterY="20"/>
            </Ellipse.RenderTransform>
        </Ellipse>
    </ControlTemplate>
    
    <!-- Indicator Scale -->
    <ControlTemplate x:Key="IndicatorScale">
        <Grid x:Name="indicatorScale" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Tag="{TemplateBinding Tag}">
            <Grid.RenderTransform>
                <RotateTransform CenterX="{Binding ElementName=indicatorScale, Path=Width, Converter={StaticResource HalveConverter}}" CenterY="{Binding ElementName=indicatorScale, Path=Height, Converter={StaticResource HalveConverter}}" Angle="{Binding Degree}"/>
            </Grid.RenderTransform>

            <Separator Width="{TemplateBinding Width}" Height="1"/>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Height="{TemplateBinding Height, Converter={StaticResource HalveConverter}}" Width="2">
                <Separator.Margin>
                    <MultiBinding Converter="{StaticResource IndicatorScaleConveter}" ConverterParameter="-9">
                        <Binding ElementName="indicatorScale" Path="Tag"/>
                    </MultiBinding>
                </Separator.Margin>
            </Separator>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Height="{TemplateBinding Height, Converter={StaticResource HalveConverter}}" Width="2">
                <Separator.Margin>
                    <MultiBinding Converter="{StaticResource IndicatorScaleConveter}" ConverterParameter="-9">
                        <Binding ElementName="indicatorScale" Path="Tag"/>
                    </MultiBinding>
                </Separator.Margin>
            </Separator>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Height="{TemplateBinding Height, Converter={StaticResource HalveConverter}}" Width="1">
                <Separator.Margin>
                    <MultiBinding Converter="{StaticResource IndicatorScaleConveter}" ConverterParameter="-7">
                        <Binding ElementName="indicatorScale" Path="Tag"/>
                    </MultiBinding>
                </Separator.Margin>
            </Separator>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Height="{TemplateBinding Height, Converter={StaticResource HalveConverter}}" Width="2">
                <Separator.Margin>
                    <MultiBinding Converter="{StaticResource IndicatorScaleConveter}" ConverterParameter="-5">
                        <Binding ElementName="indicatorScale" Path="Tag"/>
                    </MultiBinding>
                </Separator.Margin>
            </Separator>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Height="{TemplateBinding Height, Converter={StaticResource HalveConverter}}" Width="2">
                <Separator.Margin>
                    <MultiBinding Converter="{StaticResource IndicatorScaleConveter}" ConverterParameter="-3">
                        <Binding ElementName="indicatorScale" Path="Tag"/>
                    </MultiBinding>
                </Separator.Margin>
            </Separator>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Height="{TemplateBinding Height, Converter={StaticResource HalveConverter}}" Width="2">
                <Separator.Margin>
                    <MultiBinding Converter="{StaticResource IndicatorScaleConveter}" ConverterParameter="-1">
                        <Binding ElementName="indicatorScale" Path="Tag"/>
                    </MultiBinding>
                </Separator.Margin>
            </Separator>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Height="{TemplateBinding Height, Converter={StaticResource HalveConverter}}" Width="2">
                <Separator.Margin>
                    <MultiBinding Converter="{StaticResource IndicatorScaleConveter}" ConverterParameter="1">
                        <Binding ElementName="indicatorScale" Path="Tag"/>
                    </MultiBinding>
                </Separator.Margin>
            </Separator>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Height="{TemplateBinding Height, Converter={StaticResource HalveConverter}}" Width="2">
                <Separator.Margin>
                    <MultiBinding Converter="{StaticResource IndicatorScaleConveter}" ConverterParameter="3">
                        <Binding ElementName="indicatorScale" Path="Tag"/>
                    </MultiBinding>
                </Separator.Margin>
            </Separator>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Height="{TemplateBinding Height, Converter={StaticResource HalveConverter}}" Width="2">
                <Separator.Margin>
                    <MultiBinding Converter="{StaticResource IndicatorScaleConveter}" ConverterParameter="5">
                        <Binding ElementName="indicatorScale" Path="Tag"/>
                    </MultiBinding>
                </Separator.Margin>
            </Separator>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Height="{TemplateBinding Height, Converter={StaticResource HalveConverter}}" Width="2">
                <Separator.Margin>
                    <MultiBinding Converter="{StaticResource IndicatorScaleConveter}" ConverterParameter="7">
                        <Binding ElementName="indicatorScale" Path="Tag"/>
                    </MultiBinding>
                </Separator.Margin>
            </Separator>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Height="{TemplateBinding Height, Converter={StaticResource HalveConverter}}" Width="2">
                <Separator.Margin>
                    <MultiBinding Converter="{StaticResource IndicatorScaleConveter}" ConverterParameter="9">
                        <Binding ElementName="indicatorScale" Path="Tag"/>
                    </MultiBinding>
                </Separator.Margin>
            </Separator>
        </Grid>
    </ControlTemplate>

</ResourceDictionary>